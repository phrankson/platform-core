pulumi>=3.0.0,<4.0.0
pulumi-command>=0.9.0
pulumi-kubernetes>=4.0.0
pyyaml>=6.0

# Lint / static analysis, used by .circleci/config.yml's lint-code and
# static-analysis commands.
black>=24.0
mypy>=1.8
isort>=5.13
ruff>=0.5
bandit>=1.7
